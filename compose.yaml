version: '3.9'

services:
  articleservice:
    build:
      context: ./ArticleService
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

  commentservice:
    build:
      context: ./CommentService
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    depends_on:
      - profanityservice

  profanityservice:
    build:
      context: ./ProfanityService
      dockerfile: Dockerfile
    ports:
      - "8082:8080"
